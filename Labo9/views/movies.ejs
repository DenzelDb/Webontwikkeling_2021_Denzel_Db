<%- include('header'); %>
<p class="click">Click on a movie title to check out the score it got.</p>
        <div class="table">
            <ul>
                <% for(let i = 0; i < movies.length; i++) { %>
                    <li>
                        <a href="/movies/<%= i %>" class="movies"><%= movies[i].name %></a>
                    </li>
                <% } %>
            </ul>
        </div>
        <a href="/" class="back">Back</a>
    </body>
</html>